OPTIONS_ENUM <- list(
  "dynamic_options.log_level" = list(allowed_values = c("DEBUG", "INFO", "WARN", "ERROR", "FATAL")),
  "dynamic_options.use_cache" = list(type = "logical"),
  "dynamic_options.cache_dir" = list(type = "character")
)
